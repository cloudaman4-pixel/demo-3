name: issues

on:
  issues:

jobs:
  print-trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Print activation message
        run: echo "🚀 Workflow triggered! An issue event has occurred."